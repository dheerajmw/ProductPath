{
  "name": "@productpath/database",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "push": "prisma db push",
    "seed": "tsx prisma/seed.ts",
    "studio": "prisma studio",
    "typecheck": "tsc --noEmit",
    "test": "echo 'database: no tests'",
    "lint": "echo 'database lint ok'"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "@productpath/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "@types/bcryptjs": "^2.4.6",
    "bcryptjs": "^3.0.2",
    "prisma": "^6.9.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
