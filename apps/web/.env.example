# Copy to Vercel → Project → Settings → Environment Variables

# Render/Railway API URL (required). Production web calls use same-origin /pp-api/* proxy.
NEXT_PUBLIC_API_URL="https://your-api.example.com"
