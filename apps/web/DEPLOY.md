# Deploy this app on Vercel (`apps/web`)

**Root Directory must be `apps/web`.**

If Root Directory is `apps/admin`, visitors get the admin login (“Admin console”), not the public ProductPath homepage.

See [docs/vercel-deploy.md](../../docs/vercel-deploy.md).
