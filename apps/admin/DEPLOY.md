# Not the public marketing site

This is the **internal admin console** (localhost:3001 in dev).

For the candidate/recruiter UI deployed on Vercel, use **`apps/web`** as the Vercel Root Directory.

Do not use this folder for your main public Vercel URL unless you intentionally want admin-only access.
